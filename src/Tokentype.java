/**
 * Enum type enumerating different possible types of tokens
 * @author Ankit
 *
 */
public enum Tokentype {
	IDENTIFIER, NUMBER, RESERVED_WORD, SYMBOL, STRING, META_STATEMENT, DELIMITER, EXPRESSION
}