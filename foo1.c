#include <stdio.h>
#define read(x) scanf("%d",&x)
#define write(x) printf("%d\n",x)
#define print(x) printf(x)

int array[16];

int main(void)
{
    int idx, bound;
    if (idx[bound] == -1)
	{
		print("hello");
	}
}
