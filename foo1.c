#include <stdio.h>
#define read(x) scanf("%d",&x)
#define write(x) printf("%d\n",x)

int sub(int m, int n, int k);

int add(int a, int c) {
  int b;
  if(a == 2) {
    read(b);
    if(c == 4) {
    	print("b");
    }
  }
  return a+b;
}

int main(int x, int y) {
  int a, b;
  read(a);
  if(x/2 >= 5) {
    write(add(a));
  }
  if(x/3 == 9) {
    read(x);
  }
}
